import React, { useState } from 'react';

import {
  Container, Logo, Header, MainTitle, SubTitle, InputField, Input
} from './styles';

const Home = () => {
  const [value, onChangeText] = useState('Número de telefone');
  return (
  <Container>
  </Container>
)};

export default Home;
